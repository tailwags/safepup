---
import DashBoard from '@/components/DashBoard';
import Base from '@/layouts/Base.astro';

const session = Astro.locals.session;

if (!session) {
	return Astro.redirect('/login');
}
---

<Base>
	<DashBoard initialSession={session} client:load />
</Base>
